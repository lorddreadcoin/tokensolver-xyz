{
  "name": "@solguard/core",
  "version": "1.0.0",
  "description": "Core types, schemas, and scoring logic for SOLGuard Protocol (TSV-1)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["solana", "defi", "security", "token", "verification"],
  "author": "TokenSOLver",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@noble/hashes": "^1.3.0"
  }
}
